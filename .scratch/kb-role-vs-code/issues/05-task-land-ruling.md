# 05 · task：按工单 04 的裁定落地（若裁定为 A / C）

Type: task
Status: open
Blocked by: 04

## 做什么

按工单 04 裁定的落点清单实施。方向只能是**加严**（ADR-046）。

## 纪律

- 先做「接口影响面」决断：优先选**不动既有 `boolean` 语义**的最小改动（见工单 03 第 1 问的输出），避免为一条加严改动 15 个调用点的签名。
- 如果裁定要求 role 参与闸：错值不得静默放行；缺失成员行一律按现状 403；`super_admin` 旁路不变。
- **实测红名单**：先跑全量拿真实红，再改测例（只注入桩 / 只改装载，**断言一字不改**）——前图规矩。
- **反证实验**：把新判定临时短路，确认新测例**全红**，还原后**全绿**，不留残留。
- 新测例只进 `apps/api/tests/<能力>/<意图>.test.ts`（头注释「目标 / 需求 / 被测 / 简介」简体中文），并登记 `apps/api/tests/index.md`。

## 验收

- `pnpm check-types` + `pnpm lint`（零 warning）+ 相关包测试全绿。
- 反证实验有可复现命令与观察输出。
- 未落地的部分写清「做了什么 / 未做什么」。
