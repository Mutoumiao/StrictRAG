# 回写覆盖表：11 行过期标注 + 4 处汇总矛盾 + 23 行阻塞登记

Type: task
Status: resolved
Blocked by: —

## Question

`docs/testing/coverage.md` 与其四分册（`docs/testing/coverage/00-ask.md` · `01-ingest.md` · `02-acl.md` · `03-ops.md`）今天把**已有测例的行**写成欠债，又把四处汇总数写错。按行级事实回写，并给被阻塞的行留下出处明确的登记。

三件事：

1. **回写 11 行已过期的状态**（只改状态列，不新增测例）：E4 · E5 · L7 · V3 · O4 · R10 · AA1 · AB8 · AC7 · V4 · Q4 —— 每行都已在 `research/coverage-partial-tests.md` 的「A. 已具备但表中仍标部分测」表里给出可指认测例路径。
2. **修四处汇总与行级矛盾**：ask（汇总 24/26 vs 行级 25/25）· ingest（汇总 33 vs 分册 31）· acl（汇总 37 vs 行级 39，且分册「延后 2」在行级无对应）· ops 的 AD / G 子表未随行级更新。
3. **登记 23 行不可离线补的行**，每行写明阻塞方与出处，不得含糊：真 ES / 真双节点 / 真进程（E1 · E2 · H5b · H5d · P1 · AC4）· 人签与 live 真跑（T4 · C1–C3）· **源码与 Then 不一致或源码无落点（D-拼句 · H1 · H5e · M8 · S1 · S4 · Y6 · X4 · X5 · R9 · T6 · V5 · U8/J7c · P3 · AC2）**。

约束：覆盖表是**派生对照表**，改它**不得**抬 `docs/module-status` 的成熟度，**不得**改写剧本原文，**不得**把 mock 结果标成「已测」来清零。被源码阻塞的行要显式登记（改源码或回 PRD 裁口径）——这一簇正是雾中新出现的那块。

## Answer

三件事都做完了，并对**两处发现**做了改判与加改。

**1 · 11 行过期标注**：改 `已测` 10 行（E4 · E5 · V4 · Q4 · V3 · AA1 · O4 · R10 · AB8 · AC7），每行补可指认测例路径与断言要点。**只有 L7 没敢写「已测」**：清理 job 与三条护栏已落（`orphan-clean.test.ts` / `orphan-clean-on-failure.test.ts`），但 Then 里的「**周期触发**」本仓**无调度基建**、真 ES 侧清理属 B8 → 由 `缺实现` 降为 **`部分测`**，并把剩下的那一小截写进「缺口」列，而不是笼统写「部分测」。

**2 · 四处计数矛盾**：ask `24/26` → `25/25`；ingest 汇总 `33` → 分册行级 `31`；acl `22/37/延后 2` → `24/39/延后 0`（分册「延后 2」在行级**无对应行**）；ops 改用行级（原汇总沿用了未更新的 AD / G 子表）。**做法**：用脚本按 `|` 切分、取「覆盖」列、只认 6 个闭集值、排除表头行后机械重数，**以行级为准**，不为了迁就旧数去改行级。

**3 · 23 行阻塞登记**：全部写进各分册表格的「缺口」列。真 ES / 真双节点 / 真进程 6 行（E1 · E2 · H5b · H5d · P1 · AC4）写「阻塞方 + 出处」；T4 与签字剧 C1–C3 写「live 真跑 + 人签」；**16 行「源码与 Then 不一致 / 源码无落点」统一写「源码侧待定：先裁清哪一侧错，再决定改源码还是回 PRD 裁口径」并给源码出处**（`route-rules.ts:32-77` · `env.ts` · `es-sparse.ts` · `model-gateway.ts:176-214` · `adr06-snapshot.ts` 等），并明写「**禁止**写成『待补测』」。索引里那段已过期的 QUAL-* 调度指针也按实际结论改写（`B1-A4` / `G3` / `R4` / `R6-b` 如实标「已划出」及理由，`M7` 仍归 QUAL-2）。

**4 · 收口时的两处加改（本图自己发现的）**

- **S6 改判 `缺测` → `缺实现`**：守卫补测（工单 [10](./10-tests-batch-4-guards.md)）核出 admin 层**根本没有**「按当前 KB 角色裁菜单」的实现 —— 菜单 = `clipMenuForShell(me.permissions)`（`apps/admin/src/components/admin-shell.tsx:76-83`），`/auth/me` 无 `byKb`（`packages/contracts/src/auth/session.contract.ts:53-66` 注释「本批不返回 byKb」），切库只写 localStorage。覆盖表原写「缺测（源码可换 KB id）」与源码不符。已把「缺口」列改写成事实 + 两条让它有落点的路径（给 `/auth/me` 加 `byKb`，或把断言移回 api 的 S5）。
- **N2 / Z3 / C4 三行回写 `已测`**（工单 10 补测落地），并把 `coverage.md` 的「待盘点缺测」说明、`02-acl.md` / `03-ops.md` 的计数表与合计一并重算。

**收口后的行级统计（脚本重数，排除计数表行）**

| 分册 | 步骤数 | 已测 | 部分测 | 缺测 | 缺实现 | 延后 | UAT |
|------|:-----:|:----:|:-----:|:----:|:-----:|:----:|:---:|
| ask | 64 | 25 | 25 | 0 | 1 | 11 | 2 |
| ingest | 53 | 18 | 30 | 0 | 1 | 4 | 0 |
| acl | 69 | 25 | 39 | 0 | 2 | 0 | 3 |
| ops | 93 | 30 | 28 | 0 | 3 | 23 | 9 |
| **合计** | **279** | **98** | **122** | **0** | **7** | **38** | **14** |

**P2 必签 `缺测` 已清零**（此前 3 行：N2 · Z3 · C4 补掉，S6 改判）。

**未做**：没为任何一行写假装测；没改任何行级标注去迁就旧汇总；未碰源码、`prds/`、`module-status`。
