# 反向复核：这轮回写是否引入新的高估

只读复核（2026-09-20）。口径：**源码 / 测例 / 仓内文件 > 状态文档**；镜像里凡是「改成更强结论」的行，一律先去源码找反例；找不到反例才判「成立」。
逐行核了四处镜像共 **67 处改动（§0.7 的 14 条 HALF-* 逐行核过，表内合并登记）**，结果：**成立 53 处 · 新高估 1 处 · 需收紧 13 处**。
**新高估 1 处**：`docs/module-status/db.md` 第 54 行把「`generate` 恢复可用」的硬验收写成「**仓内**跑出 `No schema changes, nothing to migrate 😴`」——工单 11 的记录明说该验收**只在仓外副本达成**、**未在仓库工作区跑过**（见该行「反证」栏）。
**需收紧 13 处**：多为「同一事实三处说法不一致」与「证据只覆盖一半」（K5 覆盖表仍写 `缺实现`、api.md 与 worker.md 的「无处置」、§0.6 L3 口径未随 §0.3 改、HALF-PARSE 指针悬空、HALF-MONGO/SMOKE/SEED 真跑无仓内记录、O4 少算第三处 ES 查询、V4 分段证据、AB8 recommended 未断言、N2 证据漏 ask 侧测例、C4「映射层已补」、admin.md 自指、spec 快照口径未随 db.md 同步）。
**未发现的坏方向**：四分册行级计数机械重数全部相符；`AUTH_ENFORCE` / `DEPT_ACL_ENFORCE` / `SESSION_REWRITE_ENABLED` / `RETRIEVE_ES_MODE` / `INGEST_ES_MODE` 在被改动的行里**没有一处**被说成已开；§2.5.2 的 11 个新指针全部指向真实存在的 issue 文件。

| 文件 | 行/ID | 改动后的声称 | 反证搜寻结果（含路径） | 判定 |
|------|-------|--------------|--------------------------|------|
| `.trellis/tasks/08-06-project-backlog/status.md` | §0.1 新增标签行｜`已关闭（划出）` 定义 | 经工单/ADR 裁定无实现对象或无口径而关闭；**不**表示已实现、**不**计入分子；对应 issue tracker 的 `wontfix` | `docs/agents/triage-labels.md` 的 `wontfix` = “Will not be actioned”，语义相符；`§0.4` 公式段已写「已关闭（划出）不计入分子」，两处自洽；裁定出处 `.scratch/close-p2-exit-gaps/issues/06-qual-acl-cap.md`（“划出范围…关闭本票”）与 `.../14-qual-g3-gold-review-gate.md` 均在 | 成立 |
| status.md | §2.5.2 表头｜`实现 6` / `已具备 2` / `登记滞后 2` / `划出 4` + 「一律按子项计」 | 计数口径改按子项，划出 3 → 4 | 按子项枚举：实现 6 = PLANE-R10/TENANT-Q/E5/AA1/L7/E4；已具备 2 = K5/R6-a；登记滞后 2 = AB8/AC7；划出 4 = R4/R6-b/ACL-CAP/G3；覆盖 11 行无重叠、无遗漏，与表内行状态对得上 | 成立 |
| status.md | §2.5.2 L329 `QUAL-K5` | 状态 **已完成**；审计口 403 + Langfuse 无读取面 | 依据成立：`apps/api/src/routes/ask.ts` 成员闸 + `apps/api/tests/ask/http-audit.test.ts:200`；全仓无 trace 读取面（`apps/api/src/obs/memory-tracer.ts` 只被 `ask-tracer.ts:4` / `obs/index.ts:52` 引用，无路由消费）。**但与另两处冲突**：`docs/testing/coverage/00-ask.md` K5 仍写 `缺实现`、`docs/testing/coverage.md` 汇总仍列「K5→QUAL-K5」为「仍缺实现」，而 `prds/12-delivery-guides/04-交付控制台.md` §0.5 #7 已改「已具备 + 证据 QUAL-K5」——三处两说。改：在 `00-ask.md` K5 行补一句「Langfuse 无读取面 → 本步骤判定为已具备（见 status.md §2.5.2 QUAL-K5）」，或把 status.md 该行退回「已具备（Langfuse 无读取面）」以免与覆盖表对立 | 需收紧 |
| status.md | §2.5.2 L330 `QUAL-E4` | 已完成；迁移 `0021` · `cross-doc-dedupe.ts` · resolve 端点 | `apps/worker/tests/ingest/cross-doc-pending-review.test.ts`：held 行落库 `dedupeStatus=pending_review`+`duplicateOf`、**不进 manifest**、报告 `conflictPairs[0]={action:'pending_review',heldChunkId}`、默认 `skip_index`、脏值回落；`apps/api/src/routes/documents/index.ts:627` resolve 路由 + `apps/api/tests/ingest/dedupe-conflict-resolve.test.ts`（winner=this 回 `reindexRequired`、非待审 400、不存在 404、坏 body 400） | 成立 |
| status.md | §2.5.2 L331 `QUAL-E5` | 已完成；`contextualize-fallback-ready.test.ts`（**未验证**真 Gateway） | 5 类故障各注入一次（500/503/畸形/空白输出/AbortError）→ chunk 走 L0 prefix、报告 `contextSource=l0_fallback` 且 `L1Ok=0/L0Fallback=1`、全链后 `status=ready`；文件头自认 fetch 为替身，故“未验证真 Gateway”的括注真实 | 成立 |
| status.md | §2.5.2 L332 `QUAL-L7` | 已完成；三条护栏；**周期调度未落地**；真 ES 侧属 B8 | `apps/worker/src/ingest/orphan-clean.ts`：`no_active_version`/`doc_ready`/在飞版保护 + `INGEST_ES_MODE != mock` 整体跳过（文件头 9 行自述「未落地」）；接线 `apps/worker/src/ingest/pipeline.ts:227`（失败后 best-effort）；测例 9 + 2 = **11 条**，与 §0.5 #8「11 条测例」相符 | 成立 |
| status.md | §2.5.2 L333 `QUAL-AA1` | 已完成；策略参数可保存+审计，旧文档 version 不变 | `apps/api/tests/kb/chunk-strategy-preserves-docs.test.ts`：PATCH 200 后既有文档夹具逐字段不变、`docWrites` 零调用、`kb_settings_audits` 恰 1 行且 diff 含 `chunkStrategy.structure_paragraph.paramOverrides`；无 diff 时 0 行 | 成立 |
| status.md | §2.5.2 L334 `QUAL-ACL-CAP` | **已关闭（划出）**；全仓无 `allowedDocIds` 生产者 | `.scratch/close-p2-exit-gaps/issues/06-qual-acl-cap.md` 明写「当前图裁定为图外/不实现」且指出 `allowedDocIds` 属图外 Not yet specified；`docs/module-status/api.md` 亦记「成员 `allowedDocIds`｜PUT 只改 `role`」→ 无生产者确实成立，未把缺实现说成已实现 | 成立 |
| status.md | §2.5.2 L335 `QUAL-TENANT-Q` | 已完成；双仓 builder 缺 `tenantId` 必抛 | `apps/api/src/services/retrieve/es-sparse.ts:45,84,128` 与 `apps/worker/src/ingest/es-http.ts:49,71` 均有 `requireTenantId`；`apps/api/tests/ask/es-builder-tenant-required.test.ts` 覆盖缺/空/纯空白 + filter 逐位不变。**边界**：worker 另有 `listIndexedChunkIds`（`es-http.ts:181`）只看 `docId`（见下方 O4 行），但本行 DoD 只点名 query/bulk builder，故成立 | 成立 |
| status.md | §2.5.2 L336 `QUAL-PLANE` | **部分**；R10 已落、R6-a 已具备、R4/R6-b 划出 | 未说满：状态是「部分」而非「已完成」，与 `coverage/03-ops.md` R4=缺实现、R6=缺实现、R10=已测的分布一致 | 成立 |
| status.md | §2.5.2 L337 `QUAL-G3` | **已关闭（划出）**；`gold.yaml` 静态手写 seed、全仓无生成器 | `apps/**/*.ts`、`scripts/*` 全仓无 `gold.yaml` 写入点（仅 fixtures 与测例引用）；`.../issues/14-qual-g3-gold-review-gate.md` 有「范围外、不实现」裁定；未把「无审核闸」写成已实现 | 成立 |
| status.md | §2.5.2 L338 `QUAL-AB8` | 已完成；弹窗已落 + 测例 | `apps/admin/src/app/(ops)/kb/settings/_components/chunk-strategy-panel.tsx` 存在、挂载点 `settings-workspace.tsx:54,452`；`apps/admin/tests/ops/chunk-strategy-panel.test.tsx` 3 条（含 `getByRole('dialog',{name:'分片策略设置'})`）。**与覆盖表分册的「保存 recommended」措辞有出入**，见下方 AB8 行 | 成立 |
| status.md | §2.5.2 L339 `QUAL-AC7` | 已完成；KB 绑 judge → 400 | `packages/contracts/src/system/model-gateway.contract.ts:52,173-179`（`isKbConsumePurpose` 收口，judge 记 issue）；`apps/api/tests/kb/kb-consume-bindings-http.test.ts:36,93,97` 断言 PB 侧 PUT judge → 400 且不落行 | 成立 |
| status.md | §1.1 L158 `P2.5-L3` / L159 `P2.5-L3A` | 改写成「**有**进程内闩后停用 rewrite（`isL3RewriteFused`） / **无**写 env 的全局关」 | 源码实证：`apps/api/src/obs/metrics.ts:72` 定义、`apps/api/src/services/ask/execute.ts:153-154` 闩后 `graphDeps.rewriteEnabled = false`；`metrics.ts:79` 注释自述「不写 env / 不写收窄窗」。口径改写未把「写 env 的全局关」说成有 | 成立 |
| status.md | §1.1 L151 `B10-followup` | 真跑数字降级为「仅存 task 叙事 · 仓内不留证 · 不得当『已具备 + 证据』引用」 | 与 `prds/12-delivery-guides/04-交付控制台.md` §0.2 / §0.5 #1 两处同口径；`artifacts/` 确被 gitignore 且本机不存在 → 降级方向正确（是收紧而非放宽） | 成立 |
| status.md | §2.5.2 十一行新指针（`05`–`15` 各 issue） | 由不存在的 `08-24-qual-*` task 改为 `.scratch/close-p2-exit-gaps/issues/…` | 11 个被引用文件全部存在（`05`–`15` 实测 OK），相对路径 `../../../.scratch/...` 从 `.trellis/tasks/08-06-project-backlog/` 出发解析到仓根，正确 | 成立 |
| `prds/12-delivery-guides/04-交付控制台.md` | §0.7 #1/#3/#6/#7/#8/#9/#11/#12/#13/#14（13 行） | 10 行状态列 `未开始` → `已完成`，并补落地指针 | 逐条核过指针：`.env.operable.example` · `scripts/up-stack.mjs`（`pnpm up:apps`）· `scripts/smoke-half.mjs` + `docs/ops/half-smoke.md` · `scripts/seed-demo.mjs` · admin/web 两端 `ClosedSelect`（`apps/admin/src/components/admin-shell.tsx`、`apps/web/src/components/ask-panel.tsx`）· `documents-workspace.tsx`（上传/lifecycle 上架）· `GET /documents/:docId/ingest-jobs`（`apps/api/src/routes/documents/index.ts:886`）· `pdf-text.ts` · `embed-http.ts` · `docs/ops/auth-enforce-pilot.md` · `settings-workspace.tsx` + `chunk-strategy-panel.tsx`；收尾句仍写「默认仍 mock / `AUTH_ENFORCE` 关」，未越界 | 成立 |
| 04-交付控制台.md | §0.7 #2 / #4 / #5（HALF-MONGO / HALF-SMOKE / HALF-SEED） | `已完成`：「compose Mongo **真起** + parse 写库冒烟」「上传→审批→入库→上架→问答 **烟测**」「演示种子」 | 指针只到「脚本/文件存在」（`apps/worker/package.json` 的 `smoke:mongo`、`docker/docker-compose.yml` 有 `mongo: mongo:7`、`scripts/smoke-half.mjs`、`scripts/seed-demo.mjs`）；**仓内无真跑记录**：`.trellis/tasks/archive/2026-08/08-21-half-mongo/task.json` 的 `notes` 空、`commit`/`pr_url` 空，`prd.md` 三条 AC 全未勾，`check.jsonl`/`implement.jsonl` 只是「读过的文件」清单。按工单、非测试、若必须坐实需跑 `docker compose -f docker/docker-compose.yml up -d mongo` + `pnpm --filter @strict-rag/worker smoke:mongo`。改：状态列写「已完成（脚本已落；真跑记录不入仓）」或在指针列补「真跑记载仅存 task 归档 `08-21-half-*`，仓内不留证」——与本文件 §0.2 B10-followup 的同一口径对齐 | 需收紧 |
| 04-交付控制台.md | §0.7 #10 | 状态 `已完成`；落地指针 `apps/worker/src/ingest/parse-*` | 指针悬空：`apps/worker/src/ingest/` 实有文件为 `bull-outcome / contextualize-http / cross-doc-dedupe / doc-lock / embed-http / es-http / es-store / extract-text / failure-webhook / idempotency / ingest-report / job-ledger / mongo-body / object-store / orphan-clean / pdf-text / pipeline / purge`，**无任何 `parse-*.ts`**；txt/md UTF-8 抽取实际在 `apps/worker/src/ingest/extract-text.ts:5-6,34-38`（`TEXT_TYPES` / `TEXT_EXT` / `extractUtf8TextLayer`）。能力在、指针错。改：把指针 `apps/worker/src/ingest/parse-*` 改为 `apps/worker/src/ingest/extract-text.ts`（txt/md UTF-8 明示） | 需收紧 |
| 04-交付控制台.md | §0.4 `P2-L` | `3 / ~4` → `3 / 5`，并写明分母来源与 `OPS-STACK` 不在分母 | 分母按总 backlog §0.2 的 P2-L 定义（`B10-followup · B10-RACI · OPS-1 / B8 live · QUAL-1`）= 5，分子 3 与行内 ✅ 一致；同时把「B10-followup（部分 · 真跑数字仅存 task 叙事）」写进说明列，未把部分算成已完成 | 成立 |
| 04-交付控制台.md | §0.5 #1 | 三态仍 `部分`；现状口径保留 live 数字但注明「`artifacts/` 被 gitignore，仓内不留证」「余下仅业务人签」 | 未把数字抬成「已具备 + 证据」，符合审计「未定前不得写成已具备+证据」；工程侧派生可核：`apps/api/src/services/eval-runs.ts:50,82,312`（`isSignoffPackageRow` / `latestSignoffPackage`） | 成立 |
| 04-交付控制台.md | §0.5 #4 | `已具备 + 证据` QUAL-1；`AUTH_ENFORCE` 仓库默认仍关，试点按策略 A 开 | `apps/api/tests/auth/enforce-401.test.ts` 存在（`docs/module-status/api.md:79` 亦记 QUAL-1 默认关）；括注明确「仓库默认仍关」，未把开关说成已开 | 成立 |
| 04-交付控制台.md | §0.5 #7 | `已具备 + 证据` QUAL-K5；K5 审计口非成员 403、Langfuse 无读取面 | 源码/测例同 status.md 行；唯一问题是与覆盖表 K5 的 `缺实现` 并列（见本文第 1 条需收紧） | 成立 |
| 04-交付控制台.md | §0.5 #8 | `已具备 + 证据` QUAL-L7；孤儿清理已落、触发落「文档 `failed`」、**周期调度未落** | 11 条测例实数相符；`orphan-clean.ts` 文件头与 `pipeline.ts:227` 触发点相符；「周期调度未落」在同一格保留（三处镜像一致，见下方交叉行） | 成立 |
| 04-交付控制台.md | §0.5 #11 | 三态仍 `缺口 B8`（QUAL-TENANT-Q 已落） | 未把整格抬成已具备；live 切片 OPS-1 与 B8 的区分保留 | 成立 |
| 04-交付控制台.md | §0.5 #18 | `已具备 + 证据` QUAL-V3；禁自审 403，approve/reject 同口径 | `apps/api/tests/ingest/no-self-approve.test.ts`：自审 approve/reject 均 403 且不写库（`approveCalls`/`rejectCalls` 为空、状态仍 pending）、他人 approve 200 且 `actorUserId` 落库、无 actor 不等同自审 | 成立 |
| 04-交付控制台.md | §0.5 #22 | `已具备 + 证据` QUAL-AA1；保存不回收既有文档 version / chunk 边界 | 同 AA1 证据（deep-equal 夹具 + `docWrites` 零调用） | 成立 |
| 04-交付控制台.md | §0.5 #25 | `已具备 + 证据` QUAL-SUPER-BOOT；写路径锁超管全码；`≠` 密码登录 / `≠` 生产 IdP | `apps/api/src/routes/platform-users-roles.ts:31,158,198`（`wouldChangeSuperAdminAwayFromFullCatalog`）+ `apps/api/tests/acl/platform-users-roles.test.ts` 存在；两个 `≠` 括注把未做的部分留下 | 成立 |
| 04-交付控制台.md | §0.5 #31 | L3 改「**有**进程内闩后停用 rewrite / **无**写 env 的全局关」 | `execute.ts:153-154` + `metrics.ts:72,79` 实证；未说成「自动熔断/自动关」全局开关 | 成立 |
| 04-交付控制台.md | §0.5 #32 | Langfuse 仍 `部分`（真 SDK 未接） | 未随 K5 一起抬成全绿，属保守；`LANGFUSE_ENABLED=false` 与 `docs/module-status/api.md:8` 相符 | 成立 |
| 04-交付控制台.md | §0.5 #33 | `已具备 + 证据（余项划出）`；R10 启动 warning + 安全默认；R4/R6-b 划出 | `apps/api/src/obs/plane-quota.ts` + `apps/api/tests/obs/plane-quota-safe-default.test.ts`（staging/production 0 → `SAFE_DEFAULT_PLANE_RPM`、只对回落平面告警、dev/test 0 仍关）；划出项在格内点名，未混入已具备 | 成立 |
| 04-交付控制台.md | §0.3 | 明确不承诺段：L3「**有**打点+告警 / **有**进程内闩后停用 rewrite / **无**写 env 的全局关 / **无**面板 / **≠**准出」 | 同 L3 实证；该行仍未把 rewrite 默认开、L2 准出写成有 | 成立 |
| 04-交付控制台.md | §0.6 L166 | `P2.5-L3A` 仍写「**有**告警 / **无**自动关 / **无**面板 / `≠`准出」 | **本次漏改**：同文 §0.3 / §0.5 #31 与 `status.md` §1.1 L158-159 都已改成「有进程内闩后停用 rewrite」，本文 §0.6 与它们对立（“无自动关”按旧口径读会被当成“连进程内闩都没有”）。改：`**P2.5-L3A** L3 护栏告警 **部分**（`l3_guard_alert_total` + Pino warn；进程比非 1h 滑窗；**有告警** / **有**进程内闩后停用 rewrite（`isL3RewriteFused`，`services/ask/execute.ts:153`） / **无**写 env 的全局关 / **无**面板 / **≠**准出）` | 需收紧 |
| 04-交付控制台.md | §0.2 / §10.1 | B10-followup 加「证据口径」子条目；§10.1 顶部补 2026-09-20 回写条目 | 变更日志只新增当天条目、未回改 08-21 等历史条目（历史条目 08-20「14 条 HALF-* 未开始」作为历史保留合理）；`≠ 改任何冻结语义` 的自我声明与本次改动性质相符 | 成立 |
| `docs/module-status/README.md` | 矩阵表头 + 新增说明段 | `能力矩阵（最近一次全量核对 **2026-09-20**；前次 2026-08-12）` + 「矩阵按行增量回写…全量核对只在专门审计时做」 | 同日的账本漂移审计（`research/ledger-drift.md` #30/#31/#33）确实逐行核过矩阵十行能力行（除 L77/L78 外判终态一致），把 09-20 记作最近一次全量核对有据；说明段自限「增量回写」不夸大全量频次 | 成立 |
| README.md | 矩阵 L81（半产品可运行行） | 「P-HALF · 已落地，2026-08-21：14 条 HALF-* 均已实现（…）；默认仍 mock / `AUTH_ENFORCE` 关（≠ 生产 ES / ≠ 真杀毒 / ≠ 可签字）」 | 所列产物逐条可核（见 §0.7 行）；末句三重 `≠` 与 `docs/module-status/api.md:8` 默认值一致；但同一「真起/烟测/种子」的实证边界问题随 HALF-MONGO/SMOKE/SEED 一并存在（见本文对应需收紧行） | 成立 |
| README.md | 矩阵 L82 | 未交付清单里「admin 设置全量 UI（docTypes/策略/KB 绑定写）」→ 「admin 设置**余量**（paramSchema 动态表单 / 平台策略 CRUD 页）」 | `docs/module-status/api.md` 同段自述「**无** 平台策略 CRUD 页 / **无** paramSchema 通用动态表单」；`settings-workspace.tsx` 确有 docTypes/策略/KB 绑定三写 → 余量写法与源码相符，未扩大「已交付」 | 成立 |
| README.md | 矩阵「入库闭环」行 | 追加「跨 doc 近重复可入审（`pending_review`）· 孤儿清理已落（**周期调度未落**）」 | 与 `worker.md:46`、`api.md:45`、`coverage/01-ingest.md` L7 行四处一致；「周期调度未落」未丢 | 成立 |
| `docs/module-status/api.md` | L10 最近更新 | 补 2026-09-19 条目（E4 resolve 端点 / 签字包读时派生 / O4 双仓闸 / AA1 保文档断言） | 四项均可核（`routes/documents/index.ts:627`、`services/eval-runs.ts:312`、两仓 `requireTenantId`、`chunk-strategy-preserves-docs.test.ts`） | 成立 |
| api.md | L161（明确未做表·入库报告完整语义） | 「**有** `pending_review` 冲突对但**无**处置 / 聚合指标、**无** admin 审阅面…」 | 同文件 L45 已把 `POST /documents/:docId/dedupe-conflicts/:chunkId/resolve` 记为已落（人工二选一 = 处置），本行却写「**无**处置」→ 同篇自相矛盾，且与 status.md `QUAL-E4`「已完成」相反。改：`…**有** `pending_review` 冲突对（带 `heldChunkId`，可经 `POST …/dedupe-conflicts/:chunkId/resolve` 人工二选一）但**无**自动处置 / **无**聚合指标、**无** admin 审阅面 / L0 vs L1 Hit@k / 「高度重复」阈值提示` | 需收紧 |
| `docs/module-status/worker.md` | L10 / L62 / L84 | 「**有** `pending_review` 冲突对（带 `heldChunkId`）但**无**处置 / 聚合指标 / 审阅面」 | 同上：worker 侧确无处置逻辑，但把整条链读成「无处置」会抹掉 api 侧已落的 resolve 端点（`routes/documents/index.ts:627`）。改：三处「无处置」改为「**无**（worker 侧）自动处置；人工二选一见 api `POST …/dedupe-conflicts/:chunkId/resolve`」 | 需收紧 |
| `docs/module-status/db.md` | L9 / L36 | 补 2026-09-19：`documents.active_index_version`（`0020`）、`chunks.duplicate_of`/`dedupe_status`（`0021`），均无默认 | `packages/db/drizzle/0020_documents_active_index_version.sql`、`0021_chunks_dedupe_review.sql` 均在；`drizzle/meta/_journal.json` idx 0–21 共 22 条；列名与 `db.md:36` 一致，未夸大为已加默认/已回填 | 成立 |
| `docs/module-status/db.md` | L54（基线快照段） | 「**基线快照（2026-09-20 已补）**…→ `drizzle-kit generate` **恢复可用**，硬验收 = **仓内**跑出 `No schema changes, nothing to migrate 😴`」 | **反证**：`packages/db/drizzle/meta/` 现为 `_journal.json` + `0000_snapshot.json` + `0021_snapshot.json`（“已补 0021、`0001`–`0020` 仍缺”与磁盘相符）；但工单原文 `.scratch/p2-exit-evidence/issues/11-drizzle-meta-baseline.md` 的 **Answer** 明写：「**硬验收（唯一指标）**：**验收副本**（…）跑 generate → `No schema changes, nothing to migrate 😴`」「**未在仓库工作区跑过 generate**（避免把 `0022_*.sql` 写进仓库）」；commit `8af62c0` 的信息亦写「硬验收在**仓外副本**达成」。即被验证的 locus 是仓外副本，镜像却把硬验收写成「仓内跑出」，读者会认为仓内已验。改：把该句改为 `→ \`drizzle-kit generate\` **恢复可用**（硬验收 = 跑出 \`No schema changes, nothing to migrate 😴\`；本次在**仓外副本**达成，**未在仓库工作区跑过**，以免把 \`0022_*.sql\` 写进仓库）；可按同一副本流程复跑：\`node <副本>/node_modules/drizzle-kit/bin.cjs generate\``（若要坐实，需跑该命令，非单测） | **新高估** |
| `docs/module-status/contracts.md` | L9 / L46 | 新增 `ResolveDedupeConflictBody/Response`；`IngestReportConflictPair` 收 `action` + 可选 `heldChunkId`；`crossDocDedupeAction`；报告「拒 Hit@k」 | `packages/contracts/src/ingest/dedupe-conflict.contract.ts:14,19`；`packages/contracts/src/ingest/ingest-report.contract.ts:11-19,37`；`packages/contracts/src/kb/kb-settings.contract.ts:69,74,115,151`。措辞由旧「**拒** pending_review」改成「有 `pending_review` 冲突对」，方向是纠低估而非拔高 | 成立 |
| `docs/module-status/admin.md` | L9 最近更新 | 新增「2026-09-19（对照 `docs/module-status/admin.md` 与源码复核：分片策略弹窗、KB 设置三写已具备；`pending_review` 审阅面**未做**）」 | 事实面成立（`chunk-strategy-panel.tsx`、`settings-workspace.tsx:452`、无 `pending_review` 二选一控件），但句式自指（在同一文件里写「对照本文件复核」）、日期 09-19 与本次 09-20 复核不同源，且此处只登记复核结论而非当天落地事实，与 api/worker/db 三条同日条目的语义不同。改：`2026-09-20（复核：分片策略弹窗 \`chunk-strategy-panel.tsx\`、KB 设置 docTypes/策略/绑定三写已具备；\`pending_review\` 审阅面未做）` | 需收紧 |
| `docs/module-status/admin.md` | L37 入库报告行 | 「冲突对只展示 `otherDocId`，**无** `pending_review` 二选一控件（该审阅面仍属雾）」 | 未把未做的审阅面说成已具备；与 `close-p2-exit-gaps/map.md` 把 admin 审阅面留雾的裁定一致 | 成立 |
| `docs/testing/coverage/*.md` | 四分册计数表 + `coverage.md` 汇总 | ask 64（25/25/0/1/11/2）· ingest 53（18/30/0/1/4/0）· acl 69（25/39/0/2/0/3）· ops 93（30/28/0/3/23/9）· 合计 279（98/122/0/7/38/14） | 机械重数（按 ID 表第 5 列）完全相符：64/53/69/93 行与逐状态计数一致；`02-acl.md` 的「延后 0」属实（行级无「延后」）；`00-ask.md` 的 24/26 → 25/25 修正有据 | 成立 |
| `docs/testing/coverage.md` | 汇总段 / 必签缺测段 | 「P2 必签 `缺测` **已清零**」；「不含建议项（S6、Z3）、不含部署检查表（N2）」已处理 | 行级 `缺测` 计数 = 0（四册实测）；N2/Z3 行确有新证据、S6 改判有源码依据 | 成立 |
| coverage.md | 「不可离线补的阻塞行」段 | 真 ES/双节点/真进程 6 行（E1 E2 H5b H5d P1 AC4）· 人签/live 4 行 · 源码与 Then 不一致或无落点 **16 行** | 机械计数相符：「源码侧待定」全文 16 处（00-ask 5 / 01-ingest 2 / 02-acl 5 / 03-ops 4），「阻塞方」全文 10 处（= 6 + T4 + C1 C2 C3）；且该段自限「禁止写成『待补测』」，未把待裁定伪装成已测 | 成立 |
| coverage/01-ingest.md | E4 | `部分测` → **已测**（worker 落库入审 + api 人工二选一） | 两侧测例实证（见 status.md E4 行）；「生产 MinHash LSH 非本阶段」的边界保留 | 成立 |
| coverage/01-ingest.md | E5 | `部分测` → **已测** | `contextualize-fallback-ready.test.ts` 五类故障 + 正例对照（含 `l1_llm` 路径，补掉了旧缺口「无 `context_source=l1_llm`」）；「真 Gateway / 真 429 属 B4 live，不在本行 Then」的边界保留 | 成立 |
| coverage/01-ingest.md | V3 | `缺实现` → **已测**（`no-self-approve.test.ts`） | 见 §0.5 #18 实证；旧缺口「`uploaded_by`/`approved_by` 列未写入」也已不成立（approve 记 actorUserId） | 成立 |
| coverage/01-ingest.md | V4 | `部分测` → **已测**（`approve-then-scan.test.ts` + `no-self-approve.test.ts:116`） | approve→scan 200 + `enqueueIngest({stage:'scan'})` 有实证；但本行 Then 还含「**其后 M/L 链可绿**」，而来源只有一个：同一格里自认「（scan 后 M/L 为分段证据：M3 内存链 + L6/L8；无单例串联）」——即 M/L 侧并无由该 approve 起步的串联证据。改：把覆盖改回 `部分测`，或把 Then 拆成「V4a approve→scan 入队（已测）」+「V4b 其后 M/L 链可绿（分段证据，见 M3 / L6 / L8）」 | 需收紧 |
| coverage/01-ingest.md | Q4 | `部分测` → **已测**（`header-too-short.test.ts`） | `apps/worker/tests/ingest/header-too-short.test.ts`：夹具长度 ≥18 且 <`INGEST_MIN_EXTRACTED_CHARS`；parse → `NO_TEXT_LAYER` + `needs_ocr`、`docs.not ready`、`manifests/chunks` 皆空；第二条再证「全文过短即使进 chunk 也不得物化成功用的清单」 | 成立 |
| coverage/01-ingest.md | AA1 | `缺实现` → **已测** | 同 §0.5 #22 实证（含反向对照「无 diff 不落审计」） | 成立 |
| coverage/01-ingest.md | L7 | `缺实现` → **部分测**（剩周期触发 + 真 ES 侧） | 方向是收紧；11 条测例实数相符；`orphan-clean.ts:9` 的「未落地」自述与该行缺口一致 | 成立 |
| coverage/02-acl.md | S6 | `缺测` → **缺实现** | 源码实证：`apps/admin/src/components/admin-shell.tsx:76` 用 `clipMenuForShell(new Set(me.permissions))`（只有全局码）、`packages/contracts/src/auth/session.contract.ts` 注「本批不返回 byKb」、`apps/admin/src/lib/kb-context.ts` 只有 localStorage → 「源码未做」成立；且明确「禁止写假测」，属保守改判 | 成立 |
| coverage/02-acl.md | Z3 | `缺测` → **已测**（`chunks-workspace.test.tsx`） | 断言逐条对上：`loadChunkList(DOC_A,{limit:50})`、3 条 preview 渲染后 `loadChunkBody` 零调用；点击后恰 1 次且为 `(DOC_A, CHUNK_1)`，并显式否定 `CHUNK_2/CHUNK_3` | 成立 |
| coverage/03-ops.md | O4 | `缺实现` → **已测**（两仓 builder 租户闸） | 两个新测例 + `requireTenantId` 实证（见 §2.5.2 TENANT-Q 行）。**但**：worker `apps/worker/src/ingest/es-http.ts:181` 的 `listIndexedChunkIds(cfg, docId)` 仍以 `query:{term:{docId}}` 发 `_search`，**无 tenantId 过滤、也不失败**；该路径正是 `orphan-clean` 的 ES 侧入口。整行 Then 写的是「无 `tenantId` 的 query builder → 单测/门禁失败」，故「全仓 query builder 都强制」仍不成立。改：在本行缺口补一句 `≠ 全仓：\`apps/worker/src/ingest/es-http.ts:181\` 的 \`listIndexedChunkIds\` 仍只按 \`docId\` 查（读路径，属 ES 侧清理/B8）；本行只主张 query/bulk builder 闸` | 需收紧 |
| coverage/03-ops.md | R10 | `缺实现` → **已测**（`plane-quota-safe-default.test.ts`） | 测例实证 dev/test 0=关、staging/production 0→`SAFE_DEFAULT_PLANE_RPM`（且真限流不是无限放行）、正数原样、告警只对回落平面；「按 PRD 不做 fail closed」与 `prds/07-models §7` 拒「无配额裸奔」的口径一致 | 成立 |
| coverage/03-ops.md | AB8 | `缺实现` → **已测** | 弹窗/保存/文案三条都有断言，但行内声称的「保存 **recommended**」未直接被断言：`chunk-strategy-panel.test.tsx` 只断言 `body.items.find(code==='fixed_window')?.enabled === true`（第三条断言的是 `paramOverrides.contextMode`）；recommended 仅存在于 UI（`chunk-strategy-panel.tsx:38,142-158` 的「按 MIME 的 recommended」下拉）与 `toPatchItems`，无对应断言。改：把证据描述写成「`chunk-strategy-panel.test.tsx`（『设置』弹窗、保存 `enabled`、`paramOverrides.contextMode`、文案『不会自动全库 reindex』；recommended 下拉有 UI、未单独断言）」 | 需收紧 |
| coverage/03-ops.md | AC7 | `缺实现` → **已测** | `PutKbConsumeBindingsBodySchema`（`packages/contracts/src/system/model-gateway.contract.ts:52,173-179`）+ `apps/api/tests/kb/kb-consume-bindings-http.test.ts`（PUT judge 400 且不落行）；旧证据里「未禁 judge」已过时 | 成立 |
| coverage/03-ops.md | N2 | `缺测` → **已测**（`docs/ops`… 实际为 `apps/worker/tests/ingest/mongo-body.test.ts`） | worker 侧三条护栏实证：`$set.text` 与明文逐字节相等、字段集锁死 `['docId','kbId','text','updatedAt']`（`ciphertext/enc/iv` 类字段出现即红）、chunk 同口径、`findDocumentBody` 原样回读。但本行 Then 还含「**ask body** 与剧本 K 单一真相仍绿」，该半侧由既有 `apps/api/tests/ask/mongo-body.test.ts`（`batchLoadChunkBodies` / `composeChunkSlice` / `runRetrieve loadBodies` 缺块 fail-closed）承载，证据栏未列。改：证据栏补 `· apps/api/tests/ask/mongo-body.test.ts（ask 侧 body 单一真相 / 缺块 fail-closed）` | 需收紧 |
| coverage/03-ops.md | C4 | `部分测` → **已测**；缺口列写「逻辑 id→uuid 映射层**已补**」 | 「已补」与仓内实况不符：无任何映射实现——`apps/api/src/scripts/run-l1-golden.ts:437` 与 `apps/worker/src/eval/run-l1-batch.ts:115` 都是 `hitAtKCase(c.expectedDocIds, evidenceDocIds)` 直比，而 `fixtures/l1/gold.yaml` 的 `expectedDocIds` 是逻辑 id（`ingest-samples/01-doc`，见 gold.yaml:10）→ 仓内直跑恒 0 命中；新增测例（`packages/contracts/tests/eval/l1-hit-at-k.test.ts:60-75`）只把「未映射不得伪命中」钉成契约，映射仍是**跑批前人工步骤**（`fixtures/l1/README.md:23` 明写「live 跑批前须…替换/映射为当前 KB 的文档 uuid」）。改：缺口列替换为 `逻辑 id→uuid 仍是**跑批前人工步骤**（\`fixtures/l1/README.md:23\`；\`fixtures/l1/gold.yaml\` 写逻辑 id，\`run-l1-golden.ts:437\` / \`run-l1-batch.ts:115\` 直比 \`evidence.docId\`）→ 未映射时 Hit@k 恒 0；本轮补的是「未映射不得伪命中」护栏（\`packages/contracts/tests/eval/l1-hit-at-k.test.ts\`：互不命中且不抛错 · 换 uuid 后命中 · 只认 trim 后全等）`（若把「已补」读作「映射层已实现」，则本行按**新高估**处理） | 需收紧 |
| 交叉核查（三处镜像） | `AUTH_ENFORCE` / `DEPT_ACL_ENFORCE` / `SESSION_REWRITE_ENABLED` / `RETRIEVE_ES_MODE` / `INGEST_ES_MODE` | 被改动的行是否把开关说成已开 | 全量 grep `docs/module-status/*.md` + `docs/testing/coverage*/**`：所有出现处均为「默认关 / 默认 `mock` / `http` 需显式配」；矩阵、§0.5 #4、§0.7 收尾句三处均写「默认仍关 / 仍 mock」→ **无一处**被改成已开 | 成立 |
| 交叉核查（L7 周期调度） | status.md §2.5.2 · 04-交付控制台 §0.5 #8 · README 矩阵 · worker.md · coverage/01-ingest L7 | 「周期调度未落 / 周期触发未落地」是否三处都在 | 四处全在（`status.md` 行末、`§0.5 #8` 现状口径、`README.md` 入库闭环行、`worker.md:46`、`coverage/01-ingest.md` L7 缺口）→ 未被某处悄悄省掉 | 成立 |
| 交叉核查（drizzle/meta 口径） | `.trellis/spec/db/backend/database-guidelines.md`（同批 5344996 改动） | 「`drizzle/meta/` 只留 `0000_snapshot.json`（`0001`–`0021` 共 21 份快照未入库）→ `db:generate` **不再产出增量**」+「口径自检：快照缺口份数 = `*.sql` 文件数减 1」 | 与 `docs/module-status/db.md:54`（已补 `0021`、`generate` 恢复可用）**直接对立**，且磁盘实况为 2 份快照（`0000`+`0021`），故「文件数减 1」公式也已失效（22-1=21 ≠ 真实缺口 20）。改：spec 该 Gotcha 末段改为 `> **Gotcha（实测）**：\`drizzle/meta/\` 现有 \`0000_snapshot.json\` 与 \`0021_snapshot.json\`（\`0001\`–\`0020\` 共 20 份未入库），\`db:generate\` 已可用（验收在仓外副本达成）；口径自检：未入库份数 = \`packages/db/drizzle/*.sql\` 文件数 − \`meta/*_snapshot.json\` 文件数` | 需收紧 |

## 附：本次复核的方法与已知局限

1. **只读**：未改任何镜像 / 源码 / 覆盖表 / map / 工单，未 `git add` / `commit` / `checkout`；`prds/` 与 `.trellis/tasks/` 未入版本库，取不到改动前状态，故一律以**磁盘现状 + 源码**对账，不依赖 diff。
2. **未跑测试**：所有判据取自源码与测例文本（含文件头「目标/简介」）。表内唯一需要**跑命令**才能坐实的是 `db.md` L54 那条（`drizzle-kit generate`），已注明复跑方式；另有 `app:worker` / `apps/admin` 等测例未实际执行，若判据与运行结果冲突，以运行结果为准。
3. **保守原则**：凡「同一事实三处说法不一致」「证据只覆盖 Then 的一半」「措辞可被读成更强结论」的，一律判 `需收紧` 并给出可粘贴文本；本轮**只**判 1 处 `新高估`（`db.md` L54），因该处与工单 11 的原始记录正面冲突，不属于措辞含糊。
