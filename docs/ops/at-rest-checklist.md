# OPS-2 · At-rest 五面检查单（ADR-040 · 剧本 N）

**状态**：检查单权威路径（非「已全绿」声明）。  
**纪律**：缺项 **不得** 静默标绿；未检 / 未过 = 红或阻塞，直至 owner 签字关闭。

## 使用规则

| 规则 | 说明 |
|------|------|
| 定位 | 本文件为 at-rest 五面 **唯一** 检查入口 |
| 标绿 | 仅当该面「证据」列可核对且 owner 确认 |
| 缺项 | 标 **红 / 阻塞**；禁止用「计划中」冒充通过 |
| 签字 | 试点签字页引用本表；未绿面须列残余风险 owner |

## 五面

| # | 面 | 检查项（摘要） | Owner | 状态默认 | 证据 / 备注 |
|---|----|----------------|-------|----------|-------------|
| 1 | **传输** | TLS 入站；禁止明文管理口暴露公网 | mutou（平台） | 🔴 未检 | 网关/证书配置；试点前勾 |
| 2 | **磁盘 / 卷** | 数据卷与备份介质加密或等价隔离 | mutou（平台） | 🔴 未检 | 云盘 CMEK / 主机 LUKS 等 |
| 3 | **对象存储（S3/RustFS）** | 桶加密；访问密钥轮换；非公读 | mutou（存储） | 🔴 未检 / B9 延期 | 默认 demo 本地盘 **≠** 生产 at-rest |
| 4 | **数据库（PG）** | 磁盘加密或云托管加密；连接 TLS；凭据不落仓 | mutou（数据） | 🔴 未检 | `DATABASE_URL` 仅环境；禁默认口令进 prod |
| 5 | **密钥与配置** | JWT/Gateway Key 仅 env/密钥库；不进镜像/日志 | mutou（安全） | 🟡 工程部分 | 代码侧 Key 不进日志（B3-W）；密钥托管仍待运维 |

## 阻塞规则（剧本 N）

1. 任一必达面为 🔴 且无签字页书面豁免 → **不得**宣称「at-rest 合规完成」。  
2. 豁免须：面编号 + 理由 + 到期日 + owner。  
3. 检查单更新后同步 `08-06-project-backlog` OPS-2 行与交付控制台 §0。

## 与实现边界

- 本 chore **不**实现全套加密栈。  
- 工程已具备：生产禁 dev JWT、Key 不落日志、AUTH 默认关（demo）等；**≠** 五面全绿。
