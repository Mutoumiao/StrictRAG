{
  "name": "@strict-rag/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"[scaffold] worker: no HTTP port — BullMQ not implemented (Phase 0)\"",
    "build": "tsc --noEmit",
    "check-types": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "start": "echo \"[scaffold] worker: not implemented\""
  },
  "dependencies": {
    "@strict-rag/contracts": "workspace:*",
    "@strict-rag/db": "workspace:*"
  },
  "devDependencies": {
    "@strict-rag/eslint-config": "workspace:*",
    "@strict-rag/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:"
  }
}
