/**
 * @strict-rag/ui
 * 优先按子路径导入（对齐 partner）：
 *   import { cn } from '@strict-rag/ui/lib/utils'
 *   import '@strict-rag/ui/theme.css'
 * 组件在 Phase 接前端时按需增加 exports（button/card/…）。
 */

export { cn } from './lib/utils.js';
